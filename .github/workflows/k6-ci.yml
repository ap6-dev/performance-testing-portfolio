name: k6 Load Test

on:
  workflow_dispatch:  # manual trigger
  push:
    paths:
      - 'load-tests-k6/**'

jobs:
  load-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup k6
        uses: grafana/setup-k6-action@v1
        
      - name: Run load tests
        run: k6 run --summary-mode=full load-tests-k6/tests/api_test.js
